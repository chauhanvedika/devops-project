PORT=5000
DB_HOST=localhost
DB_PORT=5432
DB_USER=eimsuser
DB_PASSWORD=changeme_in_real_env
DB_NAME=eims
DB_SSL=false
FRONTEND_ORIGIN=http://localhost:3000

# Generate a strong random value for real deployments, e.g.:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=replace_with_a_long_random_string_never_commit_real_value
